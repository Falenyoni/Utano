# Technical Debt & Priority Plan

Consolidated 2026-07-30 from everything found while building the notifications feature and the
appointments/modals UX request, then substantially expanded 2026-08-02 after a full deliberate
audit pass across every module: Notifications, Appointments, ClinicalNotes, Identity/RBAC,
Patients, Reports, Billing, Claims, Inventory, Settings, and Financial have all now had a real
look. Dispensary specifically still hasn't had a dedicated pass — treat everything else as
reasonably thorough, not exhaustive.

Each item is tagged with status: ✅ Fixed this session / ❌ Open.

---

## Tier 0 — Licensing/compliance (resolved 2026-08-02 — free tier applies)

| # | Issue | Status |
|---|---|---|
| 0 | **MediatR is a commercial product as of v13+; Utano runs v14.1.0 unlicensed for production.** Confirmed directly — every `dotnet build`/`dotnet ef` run this session printed `LuckyPennySoftware.MediatR.License` warning: "You do not have a valid license key... If you are running in production you are required to have a licensed version." | ✅ Resolved — free Community Edition applies, license key registration pending (self-service, 2 minutes, not yet done) |

**Researched 2026-08-02 (verified against luckypennysoftware.com's own FAQ, current pricing/terms):** MediatR's dual-license model (RPL-1.5 / commercial, v13+) includes a **free Community Edition** for any organization meeting *all* of: annual gross revenue under $5,000,000 USD, total outside capital raised under $10,000,000 USD, and not a government/quasi-government/higher-education institutional user. Self-service — no approval process, no revenue proof required upfront. **Utano qualifies today** — bootstrapped, nowhere near either threshold.

**Resolution: registered for the free Community license 2026-08-04, key applied in code — fully closed.**
1. Bongani registered and got a Community Edition key (self-service, as expected).
2. **Important finding, worth remembering:** `dotnet user-secrets set "MEDIATR_LICENSE_KEY" "<key>"` alone does **not** work — confirmed empirically. MediatR's internal license check reads a real OS process environment variable directly (`Environment.GetEnvironmentVariable`), which bypasses ASP.NET Core's `IConfiguration` entirely. User-secrets only populate `IConfiguration`, so the warning stayed even with the secret correctly saved. Setting a genuine OS env var made it disappear immediately (verified side-by-side).
3. **Fix applied:** rather than requiring a real OS env var locally (inconsistent with how every other secret in this app is handled — DB password, JWT secret, ApiKey are all `IConfiguration`-based), added `cfg.LicenseKey = configuration["MEDIATR_LICENSE_KEY"];` to all 8 modules' `AddMediatR(cfg => ...)` calls (Identity, Patients, Appointments, ClinicalNotes, Inventory, Billing, Files, Notifications). This reads from `IConfiguration`, which already unifies user-secrets locally and env vars on Render — works correctly in both places with the same mechanism as everything else. Added `"MEDIATR_LICENSE_KEY": ""` placeholder to `appsettings.json`, matching the existing `ApiKey`/`ConnectionStrings` convention.
4. **Live-verified 2026-08-04**: started the API with only the user-secret set (no manual env var) — `LuckyPennySoftware.MediatR.License` warning confirmed gone from startup logs.
5. **Still needed:** add `MEDIATR_LICENSE_KEY` as an actual environment variable on Render for production (same as `ApiKey`) — not yet done, but functionally equivalent to how Render already handles every other secret, low risk.

**Paid tiers exist if Utano ever outgrows Community eligibility** (crosses $5M revenue or raises >$10M): Standard tier ~$489/year for 1–10 developers (via ComponentSource), scaling to Professional (11–50 devs)/Enterprise. At that point paying is almost certainly cheaper than migrating — MediatR is the dispatch mechanism for essentially every command/query across every module (~155 files touch `IRequest`/`IRequestHandler`/`INotificationHandler` types), so a migration would be real, non-trivial mechanical work, not a config change.

**Alternative considered and shelved (not needed given the free tier applies): migrate to the `Mediator` package** (by martinothamar — different project from MediatR). Free, MIT-licensed, source-generator based, and does support both shapes Utano uses (`IRequest`/`IRequestHandler` and `INotification`/`INotificationHandler` for the domain-events pub/sub architecture) — so it would have worked. Not a clean drop-in though: different handler delegate signature, `ValueTask` instead of `Task`, singleton-by-default lifetime, no generic request/notification support. Keep this option on file in case Utano ever exceeds the Community thresholds and migration effort at that point looks cheaper than the then-current paid tier — unlikely, but revisit with real numbers if it comes up.

**Unblocks:** Tier 1 #1 (RBAC enforcement, below) — safe to build `PermissionAuthorizationBehavior` as a MediatR `IPipelineBehavior` now, no more risk of building it twice against a future migration.

---

## Tier 1 — Security-critical (real risk today, not hypothetical)

| # | Issue | Status |
|---|---|---|
| 41 | **Audit log leaked every practice's audit trail to every other practice — cross-tenant data leak, not just cross-role.** Found 2026-08-21 (Bongani's own catch, live on production). `AuditLog` has a `PracticeId` column and practice-scoped indexes (`{PracticeId, Timestamp}`, `{PracticeId, EntityType, EntityId}`) — clearly designed to be tenant-scoped — but `ClinicalNotesDbContext.OnModelCreating` applies the global `HasQueryFilter(x => x.PracticeId == currentUserService.PracticeId)` to `Visit`/`Prescription`/`VisitProcedure`/`VisitDiagnosis`, and simply never included `AuditLog` in that list. `GetAuditLogHandler` queries `db.AuditLogs.AsQueryable()` directly with no filter of its own, so **any practice's Admin viewing the Audit Log page saw every practice's audit trail** — user names, entity IDs, actions, timestamps, across the entire platform, not just their own. Writes were always correct (`AuditService.LogAsync` sets `PracticeId` properly) — this was purely a read-side gap, and specific to this one entity; #1's audit later confirmed no other entity across any module was missing its query filter. **Compounding gap found in the same endpoint while fixing this**: `GetAuditLogQuery` had no `IRequirePermission` at all, just `[Authorize]` — any logged-in user of any role (not just Admins) could call it directly. | ✅ Fixed 2026-08-21 — added the missing `HasQueryFilter` for `AuditLog` (same pattern as the other 4 entities in this context), and added `IRequirePermission` (`settings.roles`, matching what the frontend route already gates on in `router.tsx`). No migration needed — pure query-filter + authorization-behavior fix, no schema change. Build verified clean. **Not yet deployed/live-verified** — push and confirm cross-practice isolation before considering this closed. |
| 1 | **Backend never enforces RBAC permissions.** Every endpoint checked has only `[Authorize]` (logged in) — `ICurrentUserService.HasPermission()` exists and works, but no controller/handler calls it. A Nurse can call a Billing endpoint directly (Swagger, curl, dev tools) even though the UI hides the button. This is the single largest item on this list, both in importance and in effort — it's not one fix, it's a systematic pass across every module. | ❌ Open — **design finalized 2026-07-31, not yet built.** See "Backend RBAC enforcement — design" below. |
| 30 | **Concrete, severe instance of #1: `ResetUserPasswordEndpoint` has zero permission check.** Checked the code (2026-08-02): it's `[Authorize]`-only — correctly tenant-scoped (checks `user.PracticeId != currentUser.PracticeId`), but nothing stops any logged-in staff member from resetting **any other user's password in the practice**, including the Admin's — a full account-takeover path today. Not a new mechanism to design — just the first, highest-priority endpoint to wire up once `PermissionAuthorizationBehavior` (#1) exists, ahead of `UpdateRoleCommand`/`CreateRoleCommand`. | ❌ Open — blocked on #1's mechanism landing |
| 34 | **Settings permission granularity is inconsistent — 4 distinct concerns share one permission.** Checked the code (2026-08-02): `Users`, `Roles`, `Staff`, and `Service Pricing` settings tabs each have their own distinct permission (`settings.users.view`, `settings.roles`, `settings.staff.view`, `settings.billing_config.view`). But `Practice`, `Branding`, `Medical Aid Schemes`, and `Subscription` all share the exact same permission — `settings.practice` — with no way to grant one without the others. Subscription (the most financially sensitive of the four) shouldn't be bundled in with Branding/Medical-Aid-Schemes. Fix: split into separate permissions the same way Service Pricing already got its own — natural to do alongside #4/#5 (RBAC reconciliation + permissions catalog) since it's the same authorization surface. | ❌ Open — not scoped in detail |
| 31 | **No brute-force protection on login.** Checked `LoginUserHandler` (2026-08-02): password check, generic error on failure, no failed-attempt counter, no temporary lockout, no rate limiting anywhere. Real risk today, not hypothetical — password-guessing attacks against a public login endpoint are a standard attack, not an edge case. Note the refresh-token side is solid by contrast — rotation/revocation on refresh already checked and confirmed correct, not a gap. | ✅ Fixed 2026-08-04 — two layers: (1) account-based lockout, `User.FailedLoginAttempts`/`LockedOutUntil` (new columns, migration `AddLoginLockout`), 5 failed attempts → 15-minute lockout, reset on success; (2) per-IP rate limit on `/api/auth/login` (20/hour, same rate-limiter infra as #38's signup limit) catching a distributed attack spread across many email addresses that account-lockout alone wouldn't. **Live-verified**: 5 wrong passwords → 6th attempt rejected even with the *correct* password ("Too many failed login attempts..."); confirmed via DB (`FailedLoginAttempts=5`, `LockedOutUntil` ~15 min out); simulated lockout expiry and confirmed a correct-password login both succeeds and resets both fields to `0`/`null`. |
| 2 | **`IsSystem` role has no protection.** Any Admin can rename or strip permissions from the built-in Admin role through the same generic `UpdateRoleHandler` used for custom roles — no guard preventing it. Real "nobody can log in or manage the practice anymore" risk. Small, cheap fix relative to its payoff. | ✅ Fixed 2026-07-30, user-verified live. **Needs a follow-up correction** — the design was revised 2026-07-31 (see below): the permission-editing lock on system roles was the wrong call and should be removed; the Admin-deactivation check should be generalized. Not yet applied. |
| 3 | **Backend never enforces subscription-tier feature gating.** `IFeatureService.IsEnabledAsync` (the actual per-request check) is dead code everywhere — tier gating is 100% frontend-only, same root cause as #1. Low stakes *today* (worst case: a Starter practice's own staff sees a Professional page). Becomes an actual **money** problem the moment Phase 4 (WhatsApp sending) ships, since bypassing the frontend toggle would mean paying Meta for a Starter practice's messages. Must land before Phase 4, not necessarily before everything else on this list. | ❌ Open |

### Backend RBAC enforcement — design (finalized 2026-07-31, not yet built)

**The corrected mental model, per Bongani's direction:** RBAC should be *flexible per practice* — any role, system or custom, can have its permissions freely edited by whoever is authorized to manage roles (`settings.roles`, or Admin by default). The gate belongs on **who is acting**, not on **which role is being touched**. This corrects the original #2 fix, which locked system roles' *permissions* as platform-owned — that was solving the wrong layer. What actually needed to be built was authorization enforcement (#1) in the first place; #2 becomes a byproduct of #1 rather than a separate concern.

**What stays locked on system roles, and why:** only the **name**. Renaming was explicitly ruled out ("No Renaming") because `IModuleDescriptor.GetPermissionsForRole(roleName)` — and any future reconciliation — is keyed on it. Since renaming is blocked at the API layer, `(PracticeId, Name, IsSystem = true)` is now a fully reliable, permanent key for that purpose — already backed by the existing unique index. **No new column, no hidden "marker permission," no schema change needed** — that was considered and correctly rejected as solving a problem the rename lock already solves. `RoleId` itself can't serve this purpose (it's a per-row surrogate key, unique per practice — "Admin" in two different practices has two unrelated `RoleId`s, so it can't be the cross-practice "role type" identifier `GetPermissionsForRole` needs).

**The enforcement mechanism to build:**
1. `IRequirePermission` (Core) — `string Permission { get; }`. Commands/queries implement it to declare what they need.
2. `PermissionAuthorizationBehavior<TRequest,TResponse>` (Core) — a MediatR `IPipelineBehavior` that checks `currentUser.HasPermission(...)` for any request implementing `IRequirePermission`, before the handler runs. Registered **once**, in `Utano.API/Configuration/AppConfiguration.cs` (`services.AddTransient(typeof(IPipelineBehavior<,>), typeof(PermissionAuthorizationBehavior<,>))`) — applies globally across every module's MediatR pipeline regardless of which module's `AddMediatR` call registered the handler, since it's one shared DI container.
3. `UtanoForbiddenException` (Core, new) — a permission denial is a 403, not a 400. `GlobalExceptionHandler` needs a new branch mapping this to `403 Forbidden` (currently only handles `UtanoDomainException` → 400 and everything else → 500).
4. First real usage: `UpdateRoleCommand`/`CreateRoleCommand` implement `IRequirePermission` with `Permission => "settings.roles"`. This is the natural first target given it's what triggered this whole design conversation. Every other module's commands get the same treatment incrementally after this — systematic, module-by-module, not a big-bang rewrite (per the original Tier 1 sizing note).

**Revised `UpdateRoleHandler` (corrects the 2026-07-30 fix):**
- Remove the system-role permission-editing lock entirely — any role's permissions become freely editable once `PermissionAuthorizationBehavior` has confirmed the caller holds `settings.roles`.
- Keep the system-role **name** lock as-is.
- Replace the hardcoded `role.Name == SystemRoles.Admin` deactivation check with the real invariant: **the practice must never end up with zero active roles holding `settings.roles`.** Compute this by checking whether the role being saved will still grant `settings.roles` after the update, and if not, count other active roles in the practice that do — block if that count is zero. This correctly protects against deactivating Admin, stripping `settings.roles` from Admin, *or* deactivating some other custom role that happens to be the practice's only role-manager — not just the one hardcoded name.
- `CreateRoleHandler` needs no equivalent check — creating a role never removes anyone's access.

**Reconciliation (Tier 2, #4/#5) stays additive-only** regardless of this — it only ever inserts a permission a role doesn't already have; it never re-adds something a practice deliberately removed, since there's no way to distinguish "never had it" from "removed on purpose" without extra bookkeeping that isn't being built. No conflict with per-practice permission customization.

## Tier 2 — Data integrity / compounding maintenance cost

| # | Issue | Status |
|---|---|---|
| 4 | **No RBAC seeding reconciliation.** Every permission change requires a brand-new hand-written SQL migration, forever — 11 of these exist already, one already needed a manual delete-and-reinsert cleanup (`SeedSettingsPermissions`) because nothing keeps existing practices in sync with code. Recommended fix: auto-reconcile additive permission changes on app startup (diff DB against `IModuleDescriptor.GetPermissionsForRole`, insert what's missing); keep deletions as deliberate reviewed migrations. | ❌ Open |
| 5 | **No canonical `Permissions` catalog table.** `RolePermissions.PermissionKey` is free-text `varchar(100)` with no referential integrity — a typo'd or orphaned permission key inserts silently and just never matches anything. Pairs naturally with #4 (same reconciliation pass could populate/sync this table). | ❌ Open |
| 6 | **`AppointmentSummary` has no `visitId`.** Blocks the appointments UX fixes below (#9–11) — "open the existing visit" can't work without it. Needs a small Core-abstraction + ClinicalNotes-implementation pair, same shape as the existing `IAppointmentLinker`. | ✅ Fixed 2026-07-31 — `IVisitLookup` (Core, impl in ClinicalNotes), batch-queried in `GetAppointmentsHandler`/`GetAppointmentByIdHandler`. Verified live against real data. |
| 19 | **Consultations tab's "+ Open Visit" button creates appointment-less visits, redundant with (and inconsistent with) the Walk-In flow.** Checked the code (2026-08-02): it navigates to `NewVisitPage` → `OpenVisitCommand`, where `AppointmentId` is nullable/optional — so a visit opened this way has no linked appointment at all. `WalkInPage` (Appointments tab) already covers the "patient showed up without booking" case properly — books a same-day appointment, checks it in, then the visit opens through the normal appointment-linked flow (preserving the `visitId`/billing tie-in that #6 and the RemindedAt/notifications work both assume exists). Having a second, thinner path that skips appointment creation entirely risks orphaned visits and undermines that "everything ties back to an appointment" assumption. **Open question (Bongani's call):** is there ever a legitimate case for a visit with zero appointment record (e.g. true emergency, appointment-creation-first feels like ceremony)? If not, remove the button and route everyone through Walk-In. If yes, keep it but re-label/move it so it's clearly the deliberate exception path, not a default sitting at the top of the tab. | ✅ Resolved 2026-08-04 — decision: remove it, Walk-In already covers the scenario at comparable speed and nobody had a real case for zero-appointment visits. Removed the button + now-unused `canOpenVisit`/`useAuth` from `ConsultationsPage.tsx`. `/consultations/new` and `OpenVisitCommand` deliberately left untouched — still needed by the appointment-linked "Open Visit" flow (Appointments/Waiting Room, which pass `appointmentId`). Confirmed via grep no other route reaches `/consultations/new` without an `appointmentId` now. Frontend typecheck clean. |
| 35 | **File storage (R2) has no path for practice-level assets — only patient-linked files.** Checked `docs/file-storage.md`/`FileAttachment` (2026-08-02): the documented and implemented object key scheme is `{practiceId}/{patientId}/{attachmentType}/{uuid}.{ext}`, and `PatientId` is a **required** field on the entity. There's no way to store a non-patient asset (the logo from #33, letterhead templates, consent-form templates) through this same module/bucket. Needed before #33 can actually reuse this infra rather than building a parallel one-off mechanism: extend the key scheme with a practice-level branch (e.g. `{practiceId}/_practice/{category}/{uuid}.{ext}`) and make `PatientId` optional. Everything else about the existing key design (practice-first for tenant isolation, random UUID filenames, `ConsultationId` kept as metadata not path) is correct and doesn't need changing. | ✅ Fixed 2026-08-10 — `FileAttachment.PatientId` is now `Guid?`; object key branches to `{practiceId}/_practice/{type}/{uuid}.{ext}` when null (`RequestUploadUrlHandler`). Added `FileAttachmentType.Logo`. Added `IFileAttachmentLookup` (new Core interface, implemented in Files module, DI-registered there) so other modules can resolve a FileId to a download URL / delete it without a project reference on Files — same pattern as `IVisitLookup`/`IPatientDemographicsLookup`. Added permission checks to all 4 Files endpoints (previously just `[Authorize]`, open to any logged-in user of any role): `patients.view` for list/download, `patients.edit` for upload/delete of patient files, `settings.branding` for practice-level (logo) uploads — `RequestUploadUrlCommand.Permission` branches on whether `PatientId` is null. **Also found and fixed a real pre-existing bug while doing this**: the module's original migration (2026-07-18) was scaffolded into a mistaken duplicated path `src/Modules/Modules/Utano.Module.Files/Migrations/...` and was never in the actual `Utano.Module.Files.csproj` project — confirmed via `dotnet ef migrations list --context FilesDbContext` returning "No migrations were found" and an empty `__EFMigrationsHistory` for that context. **The `FileAttachments` table has never existed in any database.** Deleted the orphaned folder, regenerated a clean `InitFilesModule` migration in the correct location. Both builds verified clean. |
| 33 | **Practice logo is stored as raw Base64, not through the existing R2 file storage.** Checked the code (2026-08-02): `BrandingEndpoints`/`UpdateBrandingCommand` takes `LogoBase64` directly, and it comes back embedded in the **login response** (`LoginUserResponse`), then gets cached in `localStorage` right alongside the JWT/auth tokens — bloating every login payload and local storage entry, capped at an arbitrary 2MB inline limit in the frontend upload handler. `Utano.Module.Files` (Cloudflare R2, presigned URLs) already exists and is fully built for exactly this kind of asset — it's just unused here (same underlying infra gap as #17, different consumer). **Fix:** upload the logo once via the existing Files endpoints, store only the resulting URL/file reference on the Practice record, and have login/branding responses return that URL instead of a base64 blob. | ✅ Fixed 2026-08-10 — `Practice.LogoFileId` (`Guid?`) replaces `LogoBase64` for all new uploads; login/branding/practice-detail responses now return `LogoUrl`, resolved fresh on every request via `IFileAttachmentLookup` (presigned URLs expire in 60 min, so this can't be denormalized/cached server-side). Frontend: `BrandingPage.tsx` uploads through the standard `POST /api/files/upload-url` → PUT-to-R2 flow (`patientId: null`, `attachmentType: 'Logo'`) instead of `FileReader`-to-base64; old logo's `FileAttachment` is cleaned up via `IFileAttachmentLookup.DeleteAsync` when replaced or cleared. Added `image/svg+xml` to the Files module's content-type allowlist (the old base64 flow accepted SVG logos; the shared upload endpoint didn't). **Presigned-URL staleness caught and fixed**: `Sidebar.tsx` previously read the logo straight from `AuthContext`/`localStorage` (set once at login) — since that's now a 60-min-expiring URL instead of permanent base64, a session left open past an hour would get a dead `<img src>`. Fixed by having the Sidebar hold a live `useQuery(['branding'], ...)` with a 45-min `refetchInterval`, falling back to the login-response URL only for the very first paint. `LogoBase64` column deliberately **kept** on `Practice` for now (not dropped) — one-off API-key-gated `POST /api/admin/practices/migrate-branding-logos` (mirrors the existing `extend-trial` admin endpoint's auth pattern) decodes any practice's existing base64 logo, uploads it to R2 server-side (new `IFileStorageService.UploadAsync`), and sets `LogoFileId`; safe to call more than once (skips practices that already have `LogoFileId` set). Drop `LogoBase64` in a follow-up migration once the backfill's been checked live. Both builds verified clean, not yet migrated/live-tested. |
| 18 | **No handling for appointments that pass their scheduled date/time without being completed or cancelled.** Checked the code (2026-08-02): `AppointmentStatus.NoShow` exists and `Appointment.MarkNoShow()` is fully implemented — but nothing in the system ever calls it. No endpoint exposes it for manual use, and no background job (unlike reminders, which do have `AppointmentReminderScanJob`) checks for and auto-applies it. Appointments left `Scheduled`/`Confirmed` past their time just sit there indefinitely — no status change, no staff-facing signal. Related but distinct failure mode: an `InProgress` appointment whose visit never gets completed (already checked in, so "no-show" doesn't apply — this is a stuck/open visit instead). **Decisions (Bongani, 2026-08-04, all recommended defaults):** (1) auto-mark `NoShow` after a 1-hour grace period past scheduled end time; (2) grace period configurable via `AppointmentNoShow:GraceMinutes` (default 60); (3) add an independent "Overdue" UI badge, visible before any automatic status change; (4) stuck `CheckedIn`/`InProgress` appointments are flagged via the same badge but never auto-transitioned (patient did show up, so `NoShow` would be factually wrong). | ✅ Fixed 2026-08-04 — Backend: `AppointmentNoShowScanJob` (Hangfire, 15-min interval, mirrors `AppointmentReminderScanJob`) + `GetAppointmentsPastNoShowGraceAsync` (deliberately scoped to `Scheduled`/`Confirmed` only, excluding `CheckedIn` even though `MarkNoShow()` technically permits it). New computed (not stored, no migration) `Appointment.IsOverdue` — covers all four active statuses (`Scheduled`/`Confirmed`/`CheckedIn`/`InProgress`) past end time — threaded through `GetAppointments`/`GetAppointmentById` responses. Frontend: replaced 3 separate client-side `isOverdue()` re-implementations (`AppointmentsPage.tsx`, `DayGridView.tsx`, `DashboardPage.tsx` — the last one was #23's flagged gap) with the single backend-computed field; added the same "Overdue" badge to `WaitingRoomPage.tsx` (the stuck-`CheckedIn`/`InProgress` case). **Live-verified 2026-08-04** against the real Render DB (QA Test Clinic sandbox, to avoid mutating real Utano Clinic appointment data): confirmed `IsOverdue` computed correctly via live API calls on two synthetic appointments (one 2h42m past end, one 27min past end); confirmed the scan job's grace-period candidate-selection query correctly distinguishes "past 60-min grace" (true) from "within grace" (false) via direct SQL replicating the repository's exact filter. **Could not verify the job's live mutation end-to-end**: discovered the production API is already running as a second Hangfire server against this same DB (`srv-d989f7l7vvec7397dhu0-hibernate-...`, started 11:53 UTC, pre-dating this deploy) — it raced my local instance for the same recurring-job slot and failed with a `TypeLoadException` since it doesn't have the new job class yet, which blocked a clean local end-to-end firing. Not a defect in the new code (confirmed via direct SQL query replication above) — just an artifact of testing locally against a DB with a live production server already attached. Will self-resolve once deployed; worth remembering for future background-job testing against this DB. Frontend typecheck clean. **Follow-up same day**: Bongani asked how appointments from previous days would ever surface, since every appointments view (including Dashboard) was hard-scoped to a single date — see #23 for the `onlyOverdue` cross-date filter that closes this gap. |

## Tier 3 — UX correctness bugs (users hit these today)

| # | Issue | Status |
|---|---|---|
| 7 | Appointments list: Reschedule/Reassign/Cancel still show when an appointment is `InProgress` — should be replaced by a single "Open" → straight to the visit. Depends on #6. | ✅ Fixed 2026-07-31 — verified live (mobile+desktop), clicking "Open" on the InProgress row navigated to the exact correct `visitId`. |
| 8 | Grid view: clicking any active-status block (including `InProgress`) always opens the Reschedule modal. Depends on #6. | ✅ Fixed 2026-07-31 — verified live, InProgress block navigates straight to the visit. |
| 9 | Waiting Room: `InProgress` → "View Visit" navigates to the generic consultations list, not the specific visit — same underlying gap as #7/#8. Depends on #6. | ✅ Fixed 2026-07-31 — same `visitId`/`navigate` pattern as #7/#8, code-reviewed rather than live-verified (Waiting Room only shows "today," no test data available for it at verification time). |
| 10 | Minor, found in passing: "Open Visit" currently shows for Scheduled/Confirmed too, not just CheckedIn. | ✅ Fixed 2026-07-31 (bonus, same lines touched by #7) — tightened to `CheckedIn` only in both list views. |
| 40 | **Login couldn't disambiguate two accounts sharing one email — the per-practice email uniqueness constraint let this state exist in the first place.** Found 2026-08-13 while confirming it was safe to reuse one real email across multiple test practices for future Resend testing — it wasn't. `GetByEmailAsync` (used at login) has no `PracticeId` filter and no deterministic `OrderBy`: `.FirstOrDefaultAsync(u => u.Email == emailVO, ...)`. If the same email existed on users in two different practices, login would silently resolve to whichever row Postgres happened to return first, and only that account's password would ever work — the other becomes practically unreachable with no error explaining why. The DB itself allowed this: `Users` had a composite `(PracticeId, Email)` unique index, not a global one — briefly made global on 2026-07-10, then deliberately reverted back to per-practice on 2026-07-22 (`RevertToPerPracticeEmailAndAddReferPermission`), for reasons not documented anywhere. `CreatePracticeCommand` (new practice + admin, both the API-key-gated and public self-signup paths) already guards against this correctly via a global `EmailExistsAsync` check in `CreatePracticeValidator` — but `CreateUserCommand` (adding a staff member to an *existing* practice, e.g. Settings → Users) only checked `EmailExistsInPracticeAsync`, scoped to the current practice — the exact same broken state was still fully reachable through that second path. | ✅ Fixed 2026-08-13 — `CreateUserValidator` now does the same global `EmailExistsAsync` check `CreatePracticeValidator` already did (removed the now-redundant per-practice check from `CreateUserHandler`). `UserConfiguration.cs`'s index reverted back to a global unique `IX_Users_Email` (migration `MakeEmailGloballyUniqueAgain`) as a DB-level guarantee, not just an application-layer one — closes the gap for any future code path that forgets the check. A cleanup script (keeps the oldest row per email, deletes newer duplicates) was handed to Bongani rather than run by me, since it deletes real rows — he reviewed the preview, ran it, and the migration went through cleanly. **Live 2026-08-14.** |
| 39 | **`NoShow` appointments have zero actions available, and there's no way to undo one.** Found 2026-08-10 while answering a direct question ("can't I edit a no-show?"). Checked the code: `AppointmentsPage.tsx` gates its entire action row (Reschedule/Reassign/Cancel/Check In) behind `ACTIVE_STATUSES = ['Scheduled', 'Confirmed', 'CheckedIn', 'InProgress']` — `NoShow` isn't in that list, so once the #18 auto-scan job (1hr grace period) flips an appointment to `NoShow`, it's a dead end in the UI with no buttons at all. Two separate gaps stacked here: (1) the backend's own `Appointment.Cancel()`/`.Reassign()`/`.Reschedule()` already work from `NoShow` (only `Confirm`/`CheckIn`/`StartVisit`/`Complete` explicitly exclude it) — the frontend just doesn't expose them; (2) there is no domain method at all to revert `NoShow` back to `Scheduled`/`CheckedIn` — if the auto-scan job gets it wrong (patient did show up, staff just hadn't checked them in yet), the appointment is permanently stuck. `NoShow` is never set manually today, only by the scan job — there's no manual "mark no-show" endpoint either. | ✅ Fixed 2026-08-22 — both gaps closed. (1) `AppointmentsPage.tsx`'s action-row gate widened to `ACTIVE_STATUSES.includes(status) \|\| status === 'NoShow'` (both mobile-card and desktop-table render blocks) — Reschedule/Reassign/Cancel now show for `NoShow`, since the backend already permitted all three. Rescheduling a no-show also naturally "undoes" it via the existing `Reschedule()` domain method, which already resets `Status` to `Scheduled` and clears `RemindedAt`. (2) New `Appointment.UndoNoShow()` domain method (`NoShow` → `CheckedIn` directly, not back to `Scheduled` — the point is getting the patient into the visit flow immediately, since they're presumably standing right there) + `UndoNoShowAppointmentCommand`/Handler/Endpoint (`PUT /api/appointments/{id}/undo-no-show`, mirrors the existing `CheckInAppointment` feature's shape exactly, including its lack of a permission check — consistent with every other appointment endpoint today, all of which are still `[Authorize]`-only pending #1's systematic RBAC pass). Frontend: new "Undo No-Show" button, shown only for `NoShow` rows, calls the new endpoint via `useUndoNoShowAppointment`. No migration — pure code, no schema change. Both builds verified clean, not yet live-tested. |
| 32 | **No self-service "Forgot Password" flow — total-lockout risk for solo Admins.** Checked the code (2026-08-02): the only reset path (`ResetUserPasswordEndpoint`) requires already being authenticated — there is no public, email-based "I forgot my password" flow anywhere in Identity. For a solo/small practice with one Admin, if that Admin forgets their own password there is currently **no way to recover the account at all** — can't log in to reset it, nothing else exists. Not a rare edge case for this SaaS's likely customer profile (small/solo practices) — a real incident waiting to happen. Needs: a public "request reset" endpoint (email + a time-limited token), an email send (ties into whatever provider gets picked for Phase 4 notifications — Resend was already the leaning choice), and a "complete reset via token" endpoint. | ❌ Open — not scoped in detail. **Email provider decided 2026-08-10: Resend** (matches the Render/Cloudflare-style dev stack, generous free tier — 3,000/month, 100/day — no meaningful free tier on Postmark, colder DX on SES). **Fully built 2026-08-16.** Domain bought (`usenemihealth.com`, Cloudflare Registrar) and Resend verified (DKIM/SPF/DMARC added manually in Cloudflare DNS, not via Resend's OAuth "Auto configure") — see `docs/infrastructure-setup.md`. Built: Core `IEmailSender` interface + `ResendEmailSender` (Notifications module, `HttpClient`-based, from address `Resend:FromEmail` config), `PasswordResetToken` entity (Identity module, SHA256-hashed token stored, never the raw value — mirrors how passwords themselves are never stored raw), `POST /api/auth/forgot-password` (rate-limited 5/hour/IP via a new `forgot-password` policy, **always returns 200 regardless of whether the email is registered or the send succeeds** — deliberate, prevents user enumeration), `POST /api/auth/reset-password` (token + new password, reuses the existing `ApplyPasswordPolicy` FluentValidation rule). Frontend: `ForgotPasswordPage`/`ResetPasswordPage`, "Forgot password?" link on `LoginPage`. Migration `AddPasswordResetTokens` — purely additive, new table only. Migrated and confirmed live 2026-08-16 (Bongani tested end-to-end). **Follow-up same day**: added security-notification emails (not verification, fire-and-forget after the fact, never blocks the password change itself) to the two existing password-change paths too — self-service `ChangePasswordHandler` and admin-triggered `ResetUserPasswordHandler` — so a user finds out if their password changed even if they didn't do it themselves (compromised session) or weren't told directly (admin reset). Both reuse the same `IEmailSender`. |
| 38 | **No self-service practice signup — the `/setup` page existed but was non-functional for real customers.** Found 2026-08-03/04 while discussing `X-Api-Key` management. `SetupPage.tsx` was already linked from `LoginPage` ("New clinic? Register a practice") but required a manual "Admin API Key" field no actual prospective customer would have — every real signup attempt would dead-end there. | ✅ Fixed 2026-08-04 — new public `POST /api/auth/register` (no API key) reusing the exact same `CreatePracticeCommand`/`CreatePracticeHandler` as the existing API-key-gated `/api/auth/setup` (kept as-is for manual/internal use, both share one source of truth for trial start/role seeding/feature seeding). Added: a duplicate-admin-email check that was missing even from the original gated endpoint (`CreatePracticeValidator`, via `IUserReadRepository.EmailExistsAsync`), and a per-IP rate limit (5/hour) on the new public endpoint via ASP.NET Core's built-in rate limiter. Frontend: removed the API-key field from `SetupPage.tsx`/`setupApi.ts`, repointed to the new public endpoint. CAPTCHA deliberately deferred — Bongani's call, rate-limiting + duplicate-email check judged sufficient for now. Both builds (backend + frontend typecheck) verified clean, not yet live-tested. |
| 37 | **No way to extend a practice's trial.** Found 2026-08-03 while verifying Phase 1's tier enforcement handles Trial correctly (it does — `StartTrial()` sets `SubscriptionTier = Professional` for the trial's duration, confirmed no gap there). Separately: `StartTrial(30)` is called exactly once, in `CreatePracticeHandler`, and never again anywhere — the existing admin endpoint (`SetPracticeSubscriptionEndpoint`) can set `Tier`/`Status`/`SubscriptionExpiresAt` but never touches `TrialEndsAt`, so there was no way to grant a customer more evaluation time short of a manual DB edit. | ✅ Fixed 2026-08-03 — `Practice.ExtendTrial(additionalDays)` (extends from the current `TrialEndsAt` if still future, else from now) + `POST /api/admin/practices/{id}/extend-trial`, same API-key-gated admin route as the existing subscription endpoint. No migration needed, reuses the existing `TrialEndsAt` column. Build-verified, not yet live-tested. |
| 36 | **Financial page shows the wrong currency symbol — actively misleading, not cosmetic.** Checked the code (2026-08-02): `FinancialPage.tsx` hardcodes `R ${fmt(...)}` throughout with `'en-ZA'` number formatting, while `BillingPage.tsx` and the Reports pages format the *exact same underlying data* (same `getRevenueSummary` endpoint) as USD (`'en-ZW'` locale, `$` prefix) — and `CreateInvoiceCommand.Currency` defaults to `"USD"`. Confirmed there's no per-practice currency field anywhere on the `Practice` entity, so this isn't intentional multi-currency support surfacing differently — it's a plain hardcode inconsistency. A practice owner looking at Financial sees their real USD revenue numbers labeled as Rand. Fix: use the same `$`/`en-ZW`/USD formatting Billing and Reports already use, everywhere on this page. | ✅ Fixed 2026-08-04 — all 21 occurrences (11 template-literal `` `R ${...}` ``, 10 JSX text-node `R {...}` that a first grep pass missed), plus the 2 stray `en-ZA` locale strings. Frontend typecheck clean, not yet visually verified in-browser (backend not involved, low risk). |
| 29 | **`NationalId` is strictly mandatory — a whole category of real patients can't be registered at all.** Checked the code (2026-08-02): `NationalId.Create()` throws if the value is empty, and `Patient.Register()` requires it unconditionally — no fallback field exists (no `PassportNumber`, no "ID pending" flag). This blocks registering: a newborn/young child without an issued national ID/birth certificate number yet, a foreign patient/tourist with only a passport number, or a walk-in without ID on them at the time (lost wallet, emergency). For a healthcare practice this isn't an edge case, it's a real category of patients the system currently cannot handle. Note the part that already works well and isn't being flagged here: duplicate detection by exact National ID match (`GetByNationalIdAsync` before registering) is solid. **Suggested fix:** make `NationalId` optional at the domain level, with either a system-generated placeholder (e.g. for newborns, backfilled once a real ID is issued) or an alternative identifier type (passport number / "ID pending"), only enforcing the duplicate-check when a real ID is actually present. | ✅ Fixed 2026-08-10 — replaced the `NationalId` value object with a typed `PatientIdentifier` (`PatientIdentifierType`: NationalId \| Passport \| Pending). Domain: `Patient.Identifier` is now a computed property over plain `IdentifierType`/`IdentifierValue` columns (not an EF `OwnsOne` — composite index across an owned type's nested property isn't supported by EF Core, confirmed by two failed attempts; see `docs/adr/0001-typed-patient-identifier.md`). Duplicate-check (`GetByIdentifierAsync`) is skipped entirely for `Pending`. `UpdatePatient` can fill in a real identifier later once issued. Migration hand-edited to backfill existing `NationalId` data into the new columns before dropping the old one (not the destructive auto-scaffolded version). Both builds verified clean. Migration run and **live-tested by Bongani 2026-08-10** — confirmed working. |
| 20 | **Patient Demographics report shows effectively no real breakdown — data bug, not a missing feature.** Checked the code (2026-08-02): the gender/age-group breakdown UI and backend (`VisitDemographicsHandler`) are fully built, including a per-doctor table. The bug is upstream: the 3 "Open Visit" navigation call sites (`AppointmentsPage.tsx` x2, `WaitingRoomPage.tsx`) — the now-primary way visits get opened, post-#7/#8/#9 — only pass `patientId, patientName, doctorId, doctorName, appointmentId, visitDate` into `NewVisitPage`'s prefill state. `patientGender`/`patientDateOfBirth` are never included, so `Visit.PatientGender`/`PatientDateOfBirth` stay null for essentially every visit opened this way. | ✅ **Correctly fixed 2026-08-05, superseding the 2026-08-04 fix below.** Bongani spotted the 2026-08-04 fix wasn't actually working on his real practice data (screenshot: 13 visits, still 100% "Unknown") and asked why gender/DOB — which live on `Patient`, not `Visit` — were being threaded through the visit-open flow at all instead of looked up live. Good catch: checked and confirmed `Visit.PatientGender`/`PatientDateOfBirth` were write-only dead weight (not shown on `VisitDetailPage`, not in `GetVisitById` — their *only* consumer was this report), added in migration `AddDemographicsToVisit` (2026-07-11) before `IPatientDemographicsLookup` existed as a pattern. **Root-cause fix**: `VisitDemographicsHandler` now calls `IPatientDemographicsLookup` directly (same batch pattern `GetAppointmentsHandler` already uses for Appointments) instead of reading frozen columns off `Visit` — correct for all visits including ones opened before any fix, no backfill needed, and self-corrects if a patient's record is ever corrected later. **Removed as dead weight**: `Visit.PatientGender`/`PatientDateOfBirth` (migration `RemoveVisitDemographicsColumns`), `OpenVisitCommand`'s two params, and the entire 2026-08-04 fix's frontend threading (`AppointmentSummary`'s two fields, `IPatientDemographicsLookup` usage in `GetAppointmentsHandler`/`GetAppointmentByIdHandler`, the 3 nav call sites, `NewVisitPage.tsx`'s `PrefillState`/`selectPatient`). **Live-verified 2026-08-05** by replicating the new query via direct SQL join against real Utano Clinic data: 9 of 14 real visits now correctly resolve real Gender/DOB (vs. 0 before the live-lookup fix). Note this doesn't prove or disprove whether the 2026-08-04 column-based fix would have worked for visits opened *after* it deployed — all 14 of these real visits predate it either way — but it's moot now since that approach is fully removed. The remaining 5 reference `PatientId`s with no matching row in `Patients` at all (not soft-deleted — no match whatsoever) — a separate, pre-existing orphaned-reference data-integrity issue, not caused by or fixable via this change; flagged for Bongani, not yet a tracked item. | ~~✅ Fixed 2026-08-04~~ — superseded, see above. Original approach: added `IPatientDemographicsLookup`, threaded `patientGender`/`patientDateOfBirth` through `AppointmentSummary` and 3 frontend nav call sites into `Visit`'s own columns. Would only ever have helped visits opened *after* that fix deployed, by design — real pre-existing visits (all 14 of them) predate it regardless. Bongani's architectural question exposed the bigger issue: gender/DOB never belonged on `Visit` at all. |

## Tier 4 — UX modernization (no correctness risk, quality-of-life)

| # | Issue | Status |
|---|---|---|
| 11 | Modal boilerplate (`ModalBackdrop`, `inputClass`, `labelClass`) duplicated across 6+ files instead of a shared component. | ✅ Fixed 2026-08-10 — extracted `shared/components/ModalBackdrop.tsx` (`maxWidth`/`closeOnBackdropClick` props) and `shared/constants/formStyles.ts` (canonical `inputClass`/`labelClass`). Applied to all files with genuine drift; a couple of local variants (`AuditLogPage.tsx`'s `w-36` search field, `DoctorScheduleModal.tsx`'s inline time-range composition) deliberately left alone — confirmed with Bongani they're real layout needs, not copy-paste drift. Frontend typecheck clean. |
| 12 | Convert `NewPatientPage`, `PatientDetailPage`, `WalkInPage`, `NewAppointmentPage` from full pages to modals, following the modal pattern already established elsewhere in the codebase (`ChangePasswordModal`, `PatientDetailPage`'s own contact/address edit modals). | ✅ Fixed 2026-08-10 — all 4 renamed/converted: `NewPatientModal`, `PatientDetailModal` use route-driven nesting (`patients/new`/`patients/:id` as children of the `patients` route in `router.tsx`, rendered via `<Outlet />` in `PatientsPage.tsx`) so they stay deep-linkable/bookmarkable/back-button-safe; `WalkInModal`, `NewAppointmentModal` use plain local component state (no dedicated URL needed) triggered from `AppointmentsPage.tsx`/`WaitingRoomPage.tsx`. Frontend typecheck clean, not yet visually verified in-browser. |
| 17 | **File upload/viewer UI doesn't exist on the frontend at all.** The backend (`Utano.Module.Files`) is fully built — Cloudflare R2, presigned URLs, all 4 CRUD endpoints, documented in `docs/file-storage.md` — but nothing in `utano-frontend` calls any of it. Needs: an upload dropzone (patient detail / consultation context), a document list, and a viewer modal (image `<img>`, PDF via `<iframe>` or a viewer component per the doc's own frontend-integration notes). Storage provider choice doesn't need revisiting — R2's zero-egress model is still the right fit for repeatedly-viewed medical images. | ✅ Fixed 2026-08-10 — new `shared/lib/files/filesApi.ts` (upload-url request, PUT-to-R2, list, download-url, delete) and `features/patients/PatientDocuments.tsx` (upload modal with type selector, list, viewer — images inline, PDFs via `<iframe>`, everything else as a download link). **Refined same day, per Bongani's call**: documents are visit-scoped, not patient-level — `PatientDocuments` lives on `VisitDetailPage` (replacing its pre-existing "Coming Soon" stub) tagging uploads with `consultationId`; `ListFilesQuery` gained a matching optional `consultationId` filter. `PatientDetailModal` no longer eager-loads Documents or "Last Visit" at all — both replaced by one lightweight "Visits & Documents → View all" button to `/patients/:id/visits`, from which a visit click reaches its own documents. **Known gap, deliberately left open**: no UI path exists anymore for a genuinely patient-level document not tied to any visit (insurance card, ID scan) — the domain model still supports it (`FileAttachment.ConsultationId` nullable), just nothing surfaces it. Not tracked as a numbered item yet since it hasn't come up as a real need. Upload/delete gated client-side on `patients.edit`. Both builds verified clean, live-verified in production for the logo path (SVG landed in R2 at the expected key after fixing a missing CORS policy — see below); documents/visit-scoping not yet live-tested. |
| 21 | **Revenue Summary report has no breakdown dimension, just one lump total per date range.** ⏸ Revisit after #24 (visit→invoice auto-linking) lands — invoice volume/linkage will look meaningfully different once that's built, worth re-checking this against real data before deciding exactly what to add. Unlike Demographics (which already gives a per-doctor table) and Visits by Doctor (grouped by doctor by definition), Revenue Summary (`RevenueSummaryHandler`) only aggregates by month — no by-doctor or by-service/category split. Genuinely useful for a multi-doctor practice wanting to see revenue per doctor. Visits by Doctor and Demographics don't need this — they're already correctly scoped as date-only/doctor-grouped. | ❌ Open — nice-to-have, not urgent |
| 24 | **No link between visit completion and invoicing — invoices are 100% manual today, with real revenue-leak risk.** Original audit note (2026-08-02) claimed Billing had zero event handlers reacting to any clinical event and that `+ New Invoice` was the only way an invoice ever gets created. **That was wrong — corrected 2026-08-04**: re-checking the code while starting Phase 4 found this was already fully built, just via direct cross-module service calls (`IBillingService.CreateDraftInvoiceForVisitAsync`/`TryAddAppointmentConsultationFeeAsync`/`AddServiceLineItemAsync`/`AddPrescriptionLineItemAsync`) rather than a `VisitCompletedEvent` handler — `OpenVisitHandler` creates the draft invoice and adds a consultation-fee line item (looked up from `ServiceItems` by appointment type) the moment a visit opens; procedures and prescriptions add their own line items as they're recorded; `CompleteVisitHandler` unconditionally ensures a draft invoice exists as a final safety net. `CreateDraftInvoiceForVisitAsync` has its own duplicate-guard (`VisitId` existence check), so it's safe to call from multiple places. **Live-verified 2026-08-04**: opened a real visit for a Consultation-type appointment, confirmed a Draft invoice was auto-created with a correct $450 "General Consultation" line item, `PatientId`/`DoctorId`/`VisitId` all correctly populated; completed the visit and confirmed no duplicate invoice was created (idempotent) and the linked appointment auto-marked Completed too. The only real gap found: **the frontend never surfaced this link at all** — `Invoice.visitId` existed in the API/DTO but nothing displayed it. **Fixed same day**: added a `visitId` filter to `GetInvoices` (backend, small additive param), a "Billing" section on the Visit detail page showing the linked invoice (number/status/total, click-through to `/billing/{id}`), and a "From Visit →" badge/link on both invoice detail views (`InvoiceDetailPage.tsx`/`InvoiceDetailModal.tsx`) back to `/consultations/{visitId}`. Live-verified the new `visitId` filter returns the correct invoice and correctly excludes non-matches. Decision on `+ New Invoice`: kept as-is, deliberate fallback for non-visit charges (product sales, re-prints, manual corrections) — Bongani's call. | ✅ Already built (backend), frontend visibility gap fixed 2026-08-04 |
| 27 | **No proper Stock Take / physical count reconciliation workflow.** Checked the code (2026-08-02): the only mechanism is `StockItem.Adjust(quantity, notes)` — a manual, one-item-at-a-time *relative* delta ("+5"/"-3") with a note, exposed as an "Adjust Stock" modal on both `InventoryPage` and `StockItemDetailPage`. No way to enter an absolute physical count and have the system compute the discrepancy itself, no way to batch multiple items into one stock-take session (a category or the whole inventory counted on one date, with a summary of total variance/shrinkage value found), and no historical record of stock takes performed (when, by whom, what discrepancies) for loss-prevention trend tracking. Every reconciliation today is a manual guess-the-delta exercise, item by item. Needs scoping: likely a new `StockTake` aggregate (session header + line items: expected qty, counted qty, computed variance), each line producing the existing `Adjust`-style `StockTransaction` under the hood so the existing transaction history stays the single source of truth. | ❌ Open — not scoped in detail |
| 28 | **No price adjustment strategy — every price change is a manual, one-item, unaudited overwrite.** `StockItem.SellingPrice`/`CostPrice` are two independent flat decimals with no markup/margin relationship encoded between them, and `UpdateDetails()` silently overwrites the old price with no history kept — can't answer "what did this item cost last month" after the fact. Missing: (1) bulk repricing (e.g. "+5% across this category" when a supplier raises costs, instead of editing every item by hand), (2) margin-based pricing (set a target markup %, auto-derive `SellingPrice` from `CostPrice` so a cost change proposes a new selling price instead of requiring two manual edits), (3) a price-change history/audit trail. | ❌ Open — not scoped in detail |
| 26 | **Medical Aid Claims page has thin filters — both gaps are frontend-only, backend already supports them.** Checked the code (2026-08-02): Claims has no separate backend module/entity — confirmed no `Claims` folder anywhere in `Utano.Module.*`. `ClaimsPage.tsx` is a filtered view of Invoices (`getInvoices({ hasMedicalAid: true, ... })`), and Submit/Approve/Reject just mutate `medicalAidName`/`medAidClaimAmount`/`medAidClaimStatus` fields that already live on the Invoice record — a reasonable lightweight design, just worth being explicit it's a Billing sub-view, not its own module. Currently only has patient-name search + a status quick-filter. Missing: (1) date range — `GetInvoicesEndpoint` already accepts `DateFrom`/`DateTo` (same as `BillingPage` already uses), `ClaimsPage`'s query call just never passes them and there's no date picker; (2) filter by medical aid scheme — useful for practices with multiple schemes, not exposed in the UI at all. | ✅ Fixed 2026-08-04 — correction: the "(2)" claim that the backend already accepted `MedicalAidId` was wrong, only `hasMedicalAid` (bool) existed; added `MedicalAidId` as a proper `GetInvoices` filter param first. `ClaimsPage.tsx` now has a scheme dropdown (`useMedicalAids`) plus `dateFrom`/`dateTo` pickers, all wired into the existing query. Live-verified the new `medicalAidId` filter (matching/non-matching cases) against the real DB. |
| 25 | **Billing invoices list has custom date-range filtering but no quick month preset.** `BillingPage.tsx` already has working From/To date pickers (confirmed — custom ranges work today). Missing: quick preset buttons (This Month / Last Month / This Year) similar to the `firstOfMonth()` pattern the Reports pages already use internally for their own defaults — cheap addition, UI-only, no backend change needed since the filter already accepts arbitrary ranges. | ✅ Fixed 2026-08-04 — added "This Month"/"Last Month"/"This Year" preset buttons next to the date pickers, same `firstOfMonth()`-style helpers as Reports. Frontend-only, typecheck clean. |
| 23 | **Dashboard stat cards are clickable but navigate to unfiltered lists, not the specific slice shown.** Checked the code (2026-08-02): `StatCard` already wraps in a `<Link>` (`DashboardPage.tsx`) — clickability itself isn't missing. The gap: "Overdue" → `/appointments` with no overdue filter applied, "No Shows" → `/appointments` not filtered to `NoShow`, "Outstanding Invoices" → `/billing` not filtered to outstanding, "Low Stock Items" → `/inventory` without the "Low stock only" checkbox pre-ticked. None of the target pages currently read an initial filter from the URL/`location.state` (`AppointmentsPage` confirmed pure local `useState`) — so this needs adding filter-from-navigation support to 3-4 pages, not just changing the `Link`s. Related: the Dashboard's "Overdue" figure was computed **client-side only** — same underlying gap as #18, fully addressed 2026-08-04 as part of that item (`onlyOverdue` filter + `/appointments?overdue=1`). **Remaining 3 cards fixed 2026-08-04**: "No Shows" → `/appointments?status=NoShow` (added a Status dropdown filter to `AppointmentsPage.tsx`, initialized from the URL — didn't exist at all before); "Outstanding Invoices" → `/billing?outstanding=1` (`BillingPage.tsx` now reads `outstanding` from the URL and actually passes it to `useInvoices` — the backend already supported the param, the frontend just never used it); "Low Stock Items" → `/inventory?lowStock=1` (pre-ticks the existing checkbox from the URL). All read-only URL-param wiring, no backend changes beyond what #26 already added. | ✅ Fixed 2026-08-04 — all 4 stat cards now link to filtered views |
| 22 | **Reports "Low Stock Alert" tab is redundant with the Inventory page's own filter.** Checked the code (2026-08-02): `InventoryPage.tsx` already has a "Low stock only" checkbox hitting the exact same `getStockItems({ lowStockOnly: true, ... })` query the Reports tab uses. The only thing the Reports tab adds is the PDF export button. Recommend: move the export button onto the Inventory page's low-stock-filtered view, drop the Reports tab. Separately noted while checking this: there is **no in-app notification for low stock at all** — `NotificationType` enum has no `LowStock` value — so this isn't currently backed by a push notification either; if that's wanted later it's a new domain event + notification type, a separate piece of work from this cleanup (not done, out of scope here). | ✅ Fixed 2026-08-04 — moved the exact same PDF export (jsPDF/autoTable, same low-stock query capped at 100 items) onto `InventoryPage.tsx`, shown next to the "Low stock only" checkbox when it's ticked; removed the `LowStockReport` component and its tab entry from `ReportsPage.tsx` entirely. Frontend-only, typecheck clean. |

## Tier 5 — Deferred by explicit decision (not forgotten, just sequenced)

| # | Issue | Status |
|---|---|---|
| 13 | Broader audit trail — Billing, Inventory, Patients, Identity have none (only ClinicalNotes does). Bongani's call: do this last, once everything else on the active list is done. | ❌ Deferred |
| 14 | Phase 4 — actual WhatsApp/email sending. Blocked on #3 (tier enforcement) landing first, plus a provider integration pass. | ✅ Email half built 2026-08-16 — `AppointmentReminderNotificationHandler` now sends email on top of the existing in-app notification. **Doctor**: only if `NotificationPreference.EmailEnabled`, email resolved via new `IUserContactLookup` (Core interface, Identity impl). **Patient**: always attempted (no per-patient consent gate exists yet — flagged, not blocking), email resolved via new `IPatientContactLookup` (Core interface, Patients impl, checks primary contact first) — silently skipped if the patient has no email on file, since SMS/WhatsApp will cover that case once built. `AppointmentReminderDueEvent` gained a `PatientId` field to make the patient lookup possible (previously only carried `PatientName` as a display string). Both failure modes (doctor email, patient email) are independently caught/logged, never block each other or the in-app notification. **Live-verified 2026-08-16** — Bongani booked a real appointment inside the reminder window with a test patient's email set, confirmed via Resend's Logs page that the send fired on the next scan tick. WhatsApp/SMS still not started — needs a provider account first. |

## Tier 6 — Minor / fix opportunistically (low stakes, cheap when touching nearby code)

| # | Issue | Status |
|---|---|---|
| 15 | `CreatePracticeHandler`'s system-role list is hardcoded instead of reading `SystemRoles.All` — cosmetic duplication risk. | ❌ Open |
| 16 | `Visit.UpdateVitals()` is dead code — defined, never called from anywhere. | ❌ Open |

## Already fixed this session

| Issue | Fix |
|---|---|
| `ICurrentUserService.UserId`/`.Email` always resolved to `Guid.Empty`/`""` (JWT claim remapping) | `MapInboundClaims = false` |
| No global exception handling — every domain exception raw-500'd with a stack trace | `GlobalExceptionHandler : IExceptionHandler` |
| `Hangfire.Core` pulled a vulnerable `Newtonsoft.Json` transitively | Pinned to 13.0.3 directly |
| ClinicalNotes audit log missed diagnosis/treatment/prescription edits | `VisitClinicalNotesUpdatedEvent` added |
| Notifications: zero wiring, no tenant check, no RBAC descriptor, free-string type, no tests, no docs | All of Phase 1–3 of `docs/notifications.md` |

---

## Recommended order (rewritten 2026-08-02, folding in the full audit pass)

**This section is also tracked as a checkable roadmap in `build-plan-2026-08.md`** — use that file to tick off progress phase by phase; this section stays as the reasoning/reference behind the order.

37 numbered items now exist (#0–#36). #6–#10 are already fixed and verified live. The rest groups into phases below by dependency and effort, not just severity — items in the same phase genuinely belong in the same work session because they touch the same files/mechanism.

### Phase 1 — RBAC pass (one connected effort — same mechanism, same authorization surface)
1. **#1** — build `IRequirePermission` + `PermissionAuthorizationBehavior` + `UtanoForbiddenException`. First real usage: `UpdateRoleCommand`/`CreateRoleCommand`.
2. **#2's correction** — apply the revised `UpdateRoleHandler` (remove the permission-editing lock, generalize the deactivation check) — this is literally part of #1's design write-up above, not separate work.
3. **#30** — wire `ResetUserPasswordCommand` to the new mechanism immediately after — highest-value endpoint to close, given it's a live account-takeover path today.
4. **#34** — split `settings.practice` into 4 distinct permissions while touching this same surface.
5. **#4 + #5** — RBAC seeding reconciliation + permissions catalog table.
6. **#3** — subscription tier enforcement — same "guard the handler" pattern, and must land before Phase 4 (#14) regardless.

### Phase 2 — Quick, high-value bug fixes (small effort, do anytime — even before Phase 1 as a warm-up)
7. **#36** — Financial currency label — trivial, minutes of work.
8. **#20** — Demographics data bug — thread gender/DOB through the 3 "Open Visit" nav call sites.
9. **#31** — Login brute-force/lockout protection.

### Phase 3 — Needs your decision before it can be scoped (bring these back to me when ready)
10. **#19** — Open Visit button: remove entirely, or keep as a clearly-labelled exception path?
11. **#18** — Overdue/No-show handling: automatic vs. manual, grace period, UI indicator, stuck-`InProgress` handling.

### Phase 4 — Revenue-integrity work (real money risk, prioritize ahead of general polish)
12. **#24** — Billing handler for the existing `VisitCompletedEvent`, auto-creates a draft invoice linked to the visit.
13. **#32** — Forgot Password flow — needs an email provider decision anyway (Resend was the leaning choice), which Phase 4/#14 will also need — worth deciding once, using it twice.

### Phase 5 — File storage chain (dependent, build in this exact order)
14. **#35** — extend the R2 key scheme for practice-level assets, make `PatientId` optional.
15. **#33** — move the practice logo onto R2, stop embedding base64 in login/branding responses.
16. **#17** — patient document upload/viewer UI — biggest of the three, do last in this chain since it reuses the same infra.

### Phase 6 — Cheap frontend-only batch (good for a lighter week, zero backend work)
17. **#25** — Billing month-preset buttons.
18. **#26** — Claims date + medical-aid-scheme filters.
19. **#22** — move Low Stock's export button onto the Inventory page, drop the Reports tab.
20. **#23** — Dashboard cards navigate to filtered views instead of unfiltered lists.

### Phase 7 — Bigger, unscoped features (design pass needed before building — not urgent)
21. **#27** — Stock Take workflow.
22. **#28** — Price adjustment strategy.
23. **#21** — Revenue Summary breakdown — stays ⏸ paused until #24 (Phase 4) actually lands.

### Phase 8 — Patient model change (bigger, touches registration form + duplicate-check logic)
24. **#29** — make `NationalId` optional with a placeholder/alternative-identifier path.

### Phase 9 — Modal cleanup (no correctness risk, whenever there's a lighter week)
25. **#11 + #12** — shared modal component, then the 4 page→modal conversions.

### Phase 10 — Deferred by explicit decision, last
26. **#13** — broader audit trail (Billing/Inventory/Patients/Identity).
27. **#14** — Phase 4 (WhatsApp/email sending) — after #3 and #32's email infra both exist.

### Fold in opportunistically — not worth a dedicated pass
- **#15, #16** — whenever nearby code gets touched anyway.
